# ===================================
# DEMO/LANDING PAGE - SERVERLESS
# ===================================

# Database Configuration
# For Vercel/Serverless deployment, use connection pooling
DATABASE_URL="postgresql://user:password@host:5432/database?pgbouncer=true&connection_limit=1"
DIRECT_URL="postgresql://user:password@host:5432/database"

# Alternative: Supabase (with connection pooling)
# DATABASE_URL="postgres://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true"
# DIRECT_URL="postgres://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:5432/postgres"

# Alternative: Neon (serverless Postgres)
# DATABASE_URL="postgresql://[user]:[password]@[endpoint].neon.tech/[database]?sslmode=require"
# DIRECT_URL="postgresql://[user]:[password]@[endpoint].neon.tech/[database]?sslmode=require"

# Alternative: PlanetScale (MySQL)
# DATABASE_URL="mysql://[username]:[password]@[host]/[database]?sslaccept=strict"

# Node Environment
NODE_ENV=development

# API Keys (if needed)
# OPENAI_API_KEY=your-openai-key-here
# GOOGLE_GEMINI_API_KEY=your-gemini-key-here

# CORS Origins
ALLOWED_ORIGINS=http://localhost:5000,http://localhost:4001,https://shahin-ai.com

# Email Configuration (for notifications)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# EMAIL_FROM=noreply@doganhub.com

# BFF (Backend for Frontend) Configuration
# Update with your actual BFF backend URL for demo registration
VITE_BFF_URL=https://api.doganhub.com

# API Configuration
VITE_API_URL=https://api.doganhub.com
VITE_DOMAIN=doganhub.com

# Vercel Configuration (auto-populated in Vercel)
# VERCEL_URL=
# VERCEL_ENV=
